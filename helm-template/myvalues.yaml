# Default values for helm-template.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
secretdata:
  amq.datasource.username: cm91dGluZy11c2Vy #routing-user
  amq.datasource.password: a1c4Y0BzeDE5Yg== #kW8c@sx19b
  minio.access.key: cm91dGluZy1lbmdpbmU= #routing-engine
  minio.secret.key: ZUAzZDRyZnk3akxleFF4MzRzd2R4cjM9 #e@3d4rfy7jLexQx34swdxr3=
  redis.datasource.password: Q0VLT2tGNnlAOVEyaFQyNFkhSnc= #CEKOkF6y@9Q2hT24Y!Jw

replicaCount: 1

image:
  repository: nginx
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets:
  - name: regcred
nameOverride: "hamid-app-1"
namespace: "hamid-test"

Affinity:
  - type: nodeAffinity
    weight:
    rule: required
    key: app
    operator: In
    values:
      - appl
  - type: nodeAntiAffinity
    rule: required
    key: app
    operator: In
    values:
      - appl2
      - llla
  - type: nodeAffinity
    rule: preferred
    key: app
    operator: In
    values:
      - appl2
  - type: nodeAffinity
    rule: required
    key: gh
    operator: In
    values:
      - asdf

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: "hamid"

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

services:
  - name: http
    type: NodePort
    port: 320123
    targetport: 8080

  - name: ssh
    type: ClusterIP
    port: 8008
    targetport: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}
